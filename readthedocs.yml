version: 2

mkdocs:
  configuration: mkdocs.yml
  fail_on_warning: false

python:
  version: 3.8
  install:
    - method: pip
      path: "mkdocs>=1.2.3"
    - method: pip
      path: .
      extra_requirements:
        - docs
